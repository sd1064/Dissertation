function [ Xs , Ys , Zs ] = extrinsicSphereCalibration( fx , fy )
%EXTRINSICSPHERECALIBRATION Calculates extrinsic paramteres using a sphere
%   Detailed explanation goes here

    % NEED us,u0,vs,v0 u0=0,v0=0
    u0=
    
    
    xs = (us-u0)/fx;
    ys = (vs-v0)/fy;
    delta = -((xs^2+ys^2)^1/2);
    theta = -atan(delta);
    A = N/(fx*fy);

    Zs = Rs*(pi/(A*cos(theta)))^1/2;
    Xs = xs * Zs;
    Ys = ys * Zs;
    
end

